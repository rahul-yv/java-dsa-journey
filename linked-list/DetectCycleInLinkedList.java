/*
 * Detect Cycle in a Linked List - I already solved this with Floyd's tortoise
 * and hare in DetectCycle.java, so I'm not duplicating the full thing.
 *
 * See: DetectCycle.java   (LeetCode #141 - Easy)
 *
 * Quick idea: slow pointer moves 1 step, fast moves 2. if there's a loop they
 * eventually meet inside it; if fast hits null, no loop.
 */
public class DetectCycleInLinkedList {
    public static void main(String[] args) {
        System.out.println("see DetectCycle.java in this same folder (Floyd's algorithm)");
    }
}
